## Agent skills

### Issue tracker

GitHub issues on `nemixe/claude-server` are used for work tracking. See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical triage roles map to the default labels (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`) and this repo also uses the workflow labels `doing` and `done`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout: root `CONTEXT.md` and `docs/adr/`. See `docs/agents/domain.md`.
